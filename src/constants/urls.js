const championSummary = "http://localhost:8000/summary";

const championDetail = "http://localhost:8000/champion";

const championIcon = "https://raw.communitydragon.org/latest/plugins/rcp-be-lol-game-data/global/default/v1/champion-icons";

const urls = {championSummary, championDetail, championIcon}

export default urls;